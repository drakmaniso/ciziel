module example-text


local example-1() =
    a = own "Hello"
    -- a :: own String

    b = own a.append(", World!")
    -- a is now invalid
    -- b :: own String

    c = b
    -- b is now invalid
    -- c :: String

    d = c.slice(1, 5)
    -- c :: String
    -- d :: String

    e = own c.append(" How are you?")
    -- e :: own String

    ()
